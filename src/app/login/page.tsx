import { SignInCard } from '@/components/ui/sign-in-card';

export const metadata = {
  title: 'Sign In',
  description: 'Sign in to your SweetDelights account to order delicious cakes and cupcakes.',
};

export default function LoginPage() {
  return <SignInCard />;
}
